program TestGIS;

uses
  Forms,
  untTest in 'untTest.pas' {Form1},
  StrUtilsUnit in '..\000 COMUNI\StrUtilsUnit.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
